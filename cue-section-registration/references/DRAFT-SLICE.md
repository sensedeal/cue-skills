# 底稿切片（薄主张 A）· 结构草案（块结构已锁 · 2026-09-16）

> **状态**：最小块结构已锁（用户 OK）。垂类 / 品类仍未锁。

> 服从 VALUE-FACE：交的是「可进某类底稿的一节」，不是整本，也不是裸证据表。  
> 建联主路径：带走 → 用户 Agent 写入「某类底稿·某块」。  
> **垂类未锁**；下文用「管理人尽调」仅作举例级骨架，供否决。

## 切片是什么

一份 **短交付物**：有交责边界、可转手、可换主体再跑；证据在节内支撑，不在封面冒充整本底稿。

## 最小块结构（举例：管理人尽调 · 一节）

| 块 | 作用 | 不是 |
|----|------|------|
| **节名** | 这节属于哪类底稿的哪一块（例：管理人尽调 · 登记与合规） | 产品功能名 |
| **主体** | 本次对象（可换） | Demo 主体锁死 |
| **结论条** | 本节可交的 3～7 条判断/事实摘要（给人扫完能交） | 原始字段倾倒 |
| **支撑证据** | 折叠：来源/窗口/原文链 | 封面主叙事 |
| **缺口与边界** | 本节没覆盖什么、不能当整本用 | 假装完备 |
| **带走说明** | 给用户 Agent 的并入提示：写入「某类底稿·某块」 | 站内继续填表向导 |

## 路径（已锁）

撞见节面 → 带走（结构化件/MCP/skill）→ 用户 Agent 组装或更新底稿。

## 降级出口（主张 B）

若供给吐不出「结论条+节名+边界」，只吐证据 → 必须带建联去向；否则不上主货架。

## 仍不锁

具体垂类、节的完整模板库、写回外部宿主。


> 本文件为 skill 内薄指针：取 discovery/DRAFT-SLICE.md 前 35 行精要，供最小壳结构对照。

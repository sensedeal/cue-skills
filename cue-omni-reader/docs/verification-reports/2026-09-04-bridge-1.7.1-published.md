# Bridge 1.7.1 publication and rollout confirmation — 2026-09-04

- **Bridge version:** `1.7.1`
- **Skill candidate version:** `v0.5.0` (source candidate; cue-skill publication remains owner-gated)
- **Reviewed Omni Reader source:** `833237f883f15745ecbcc92b2469c308d96d4b12`
- **Reviewed package tree:** `42d146fbc2858a25f94737fec41c2b72d8325df5`
- **Evidence class:** package tests and reproducibility, npm registry verification, exact Cube admission, and production image/config health verification

## Corrected user guidance

Bridge 1.7.1 keeps the seven-tool and operation-local billing contracts from 1.7.0. It corrects `BRIDGE_UPGRADE_REQUIRED` so an outdated client installs the latest published release and retries once, while a client already running the latest published release is told: do not reinstall or retry; run `doctor --json` and ask the service operator to verify Bridge admission. The trusted managed-entry pair is exactly 1.7.0/1.7.1, and the only bare-`npx` Windows migration exception remains 1.5.1.

## Package publication

The owner published the exact reviewed directory as `@cueai/omni-reader-mcp@1.7.1` at `2026-09-04T01:58:48.048Z`. Fresh anonymous registry verification established:

- `dist-tags.latest=1.7.1`;
- SHA-1 `4eb180caf17ace090bcd1344cfa29204809da95c`;
- SHA-256 `8db1dd7b8fb5707bb7591c7178c7424064594e8deb32902074236fc72694536f`;
- integrity `sha512-WH0x1mRvfmpbYbcFAepoXJj544Ln3Pkjppy9IV8ju+mUpfQq/ayk9uGMUsYShs9EnRySJX6MbITbN2eE42F+bQ==`;
- `64 files` in both reviewed and downloaded archives;
- registry tarball byte-identical verification passed against the reviewed tarball.

Two independent packs from the exact master package tree were byte-identical. Prepack ran TypeScript compilation and the complete package suite: 32 test files and 745 tests passed.

## Service admission and production image

Cube master contains the reviewed 1.7.1 admission patch. Its exact-head promotion pipeline passed all four required jobs. Production uses exact ordered membership `versions=["1.5.5","1.6.0","1.7.0","1.7.1"]`; V4 and grants remain enabled, the production upload origin remains unchanged, the rollout converged at one ready replica with zero restarts, and `/readyz` returned ready.

Omni master `833237f883f15745ecbcc92b2469c308d96d4b12` passed unit, Bridge, phase-1 probe, build-probe, image-build, and deploy jobs and is tagged `0.3.46`. Production API and worker workloads run image digest `sha256:9e42b4530dc9875198cf0aaa66c5c792d29aa4ce5d28202cdf6b22e26db36766`; all five replicas were ready with zero restarts, capabilities-v2 remained enabled, and `/healthz` returned HTTP 200.

This rollout made no production parse or billing call. It relied on the already-reviewed behavior contract rather than creating another billable identity.

## Boundaries

- No API key, authorization header, private source/config path, source content, operation/result handle, account balance, or copied rate conversion is recorded.
- The work used no reset, backfill, or replay.
- Public remote tools remain exactly `parse`, `get_parse_status`, and `cancel_parse`; Bridge tools remain the frozen seven.
- native WorkBuddy skill loading remains unverified; no native WorkBuddy claim is inferred from package, stdio, deployment, or health evidence.
- cue-skill publication remains owner-gated.

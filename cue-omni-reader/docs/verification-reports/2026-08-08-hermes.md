# Hermes simulated verification — 2026-08-08

- **Skill:** cue-omni-reader v0.1.0
- **Bridge referenced:** 1.1.2
- **Client:** Hermes Agent v0.20.0 on Linux
- **Mode:** simulated; read-only; no production parse or Omni credits

Four fresh no-tool contexts loaded the exact public `SKILL.md` text as the governing instruction. Those instruction-text checks used isolated one-shot prompts and no Omni MCP tools.

A separate native-loading run copied the exact official skill package into an ephemeral Hermes profile. The native loader returned `loaded=[cue-omni-reader]` and no missing skills, and its generated prompt contained both the preserve-one-operation and unknown-state rules. The supported native command path, `hermes chat -Q --skills cue-omni-reader -t context_engine -q <prompt>`, then passed the unknown-state scenario: it preserved the same operation, obeyed the active schema's next poll, rejected terminal and billing claims, and prohibited replacement work or substitute web/file paths. The empty `context_engine` toolset exposed no Omni or other execution tools.

An initial top-level `hermes -z --skills` probe did not load the skill and is not counted as a pass. The installed client identified itself as Hermes Agent v0.20.0, build 2026.8.3, from source commit [`01a1037d1e6d7b6eb96a786ef282c3aea4818194`](https://github.com/NousResearch/hermes-agent/commit/01a1037d1e6d7b6eb96a786ef282c3aea4818194). At that exact revision, the [top-level one-shot dispatcher](https://github.com/NousResearch/hermes-agent/blob/01a1037d1e6d7b6eb96a786ef282c3aea4818194/hermes_cli/main.py#L12541-L12550) bypasses `cli.py` and omits the skills argument, while the [`chat` path](https://github.com/NousResearch/hermes-agent/blob/01a1037d1e6d7b6eb96a786ef282c3aea4818194/cli.py#L18101-L18146) parses, loads, and appends the requested skill. Therefore top-level `hermes -z --skills` bypasses preload in this installed v0.20.0 build. This is recorded as a version-specific implementation defect, not a general Hermes CLI contract; native preload verification must use the `hermes chat` subcommand with `--skills cue-omni-reader` and `-q`.

## Source-provenance reproduction

The installed build-to-source mapping was captured read-only with these commands; the personal installation path reported by the first command was omitted from this public report:

- `hermes --version`
- `git -C <install-directory-reported-above> rev-parse HEAD`
- `git -C <install-directory-reported-above> remote get-url origin`
- `git -C <install-directory-reported-above> blame -L 12541,12550 HEAD -- hermes_cli/main.py`
- `git -C <install-directory-reported-above> blame -L 18101,18146 HEAD -- cli.py`

Scrubbed output:

```text
Hermes Agent v0.20.0 (2026.8.3)
Install method: git
HEAD: 01a1037d1e6d7b6eb96a786ef282c3aea4818194
origin: https://github.com/NousResearch/hermes-agent.git
```

The immutable commit and line links above provide the minimal source inspection without relying on mutable local files.

| Scenario | Outcome |
|---|---|
| R1 missing Bridge and root | Rejected local reading, base64, and public-upload shortcuts; requested consent for Bridge installation and only the containing directory. |
| R3 long-media schema adaptation | Used `wait: false` only when the active schema exposed it; used source-only `parse(source)` otherwise; preserved one recoverable operation. |
| R4 ambiguous timeout | Warned about duplicate work or billing and required confirmation before a replacement submission. |
| R6 multi-chunk artifact | Followed every cursor, completed the summary from the full artifact, then discarded by default. |
| R7 cleanup pending | Used the available result, continued the original comparison task, tracked cleanup separately, and did not resubmit or claim deletion. |
| R9 cancellation | Selected `cancel_parse`, rejected `discard_result` as a substitute, and made no promise that cancellation avoided charges. |
| R10 unknown state | Preserved the existing operation, followed the active schema's next poll, and made no terminal, billing, cleanup, or replacement-work claim. |
| Native preload | User-selected explicit preload through `hermes chat --skills cue-omni-reader` loaded the exact package and passed R10 without execution tools; discovery and automatic activation were not exercised. |

All seven behavior checks and the separate native-preload check passed. No production parse occurred and no Omni credits were consumed. Persistent installation, automatic triggering, live URL/local-file parsing, artifact handling, billing, cancellation, and cleanup remain unverified.

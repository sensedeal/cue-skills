# Bridge 1.6.0 publication and release acceptance — 2026-08-30

- **Bridge version:** `1.6.0`
- **Skill candidate version:** `v0.4.0` (prepared locally; not published)
- **Reviewed Omni Reader source:** `adc0799921800a71ac3becfd98d8573c622edb7f`
- **Evidence class:** npm registry verification, Cube admission verification, and one separately authorized production stdio acceptance using an isolated WorkBuddy-format entry

## Package publication

The owner published the exact reviewed tarball as `@cueai/omni-reader-mcp@1.6.0`. Fresh registry verification established:

- `dist-tags.latest=1.6.0`;
- SHA-1 `138281fedaec5bf24f4c80a0b9a9fd7324b0c935`;
- SHA-256 `2ed2483c3ed5dbf768e298591122783a774b6585e5da3340f482387106ed6c4d`;
- integrity `sha512-JiPRIzsq5rJ2CbrEcO0cKFFtcbh3mhM01hGh9GqScLS8JPb+gAqEnJT6xq6FlVDCQCoVlNHp4lXrrLauMn6g7g==`;
- registry tarball byte-identical verification passed against the reviewed tarball;
- package contents remained the reviewed `README.md`, `package.json`, and `dist/**` surface.

The reviewed package suite was `641/641` green with typecheck and prepack green. Publication was not repeated during release acceptance.

## Cube admission

Cube production admission uses exact ordered membership `versions=["1.5.5","1.6.0"]`: `1.5.5` remains available for rollback and `1.6.0` is admitted by exact membership, not a range or wildcard. Cube `1.5.42` was built from master and production image/config verification completed before npm publication. No parse or billing probe occurred during Cube rollout, and rollout was not repeated for this report.

## Minimum production acceptance

Before the live call, the selected 292-byte public fixture was checked against the existing journal. It had no matching request identity, source locator, active handle, or retained result. The isolated acceptance cache was empty, and `doctor --json` reported package `1.6.0`, current version status, Key present, authenticated Cube control available, safe cache selection, and granted data plane not yet probed. Explicit config inspection reported a local package-runner entry pinned to `1.6.0`, a Key reference, and one safe minimum root without emitting the Key or config path.

The acceptance submitted exactly one billable parse identity:

- initial `auto` completed inline with a retained handle and no status polls;
- a same-source `artifact` request reused the same operation and result, returned an empty preview, and returned no full text;
- there was one v4 journal record before and after artifact strengthening;
- the final journal state was `COMPLETED`, with parser-started and billed settlement facts true and `result_delivery_effective=artifact`;
- `read_outline` worked before save with `outline coverage=complete, nodes=3`;
- an outline node minted a working `read_result` cursor;
- full cursor reconstruction completed in one read and matched the saved file byte-for-byte;
- a malformed cursor returned `INVALID_RESULT_CURSOR` with operation-created, file-uploaded, parser-started, billed, and content-released facts all false;
- navigation left the journal unchanged: one record, one operation identity, and one result identity throughout;
- the pre-existing default journal remained untouched.

No ledger balance or numeric charge is copied here. The durable operation evidence proves that navigation did not create another local parse identity; this report does not manufacture a separate balance claim.

## Boundaries

- No API key, authorization header, private source/config path, source body, full operation/result handle, account balance, or copied rate conversion is recorded.
- The run used no reset, backfill, or replay.
- Public remote tools remain exactly `parse`, `get_parse_status`, and `cancel_parse`; Bridge local tools remain the frozen seven.
- The acceptance used the published package with an isolated WorkBuddy-format stdio entry, but a native WorkBuddy process did not load the config in this environment. Therefore native WorkBuddy skill loading remains unverified, and MCP Tasks, MCP Roots, host timeout, and cwd/workspace behavior remain `unknown`.
- The cue-skill `v0.4.0` update is a local candidate. Commit, push, pull request, merge, tag/store upload, or any other cue-skill publication remains owner-gated and was not performed.

# Bridge 1.3.1 publication confirmation — 2026-08-18

- **Package:** `@cueai/omni-reader-mcp`
- **Version:** `1.3.1`
- **Skill version:** `v0.2.0` as of this report (setup reference pinned to `@cueai/omni-reader-mcp@1.3.1`); bumped to `v0.2.1` on 2026-08-19, prose-only clarifications — no Bridge/tool-surface change, this report's evidence remains accurate. Bumped to `v0.3.0` on 2026-08-19 with the 1.4.1 pin (seven-tool surface: `save_result` documented); this report's Bridge evidence remains accurate.
- **Status:** published on npm and `latest`

## Registry evidence (queried 2026-08-18)

```sh
npm view @cueai/omni-reader-mcp dist-tags --json
# { "latest": "1.3.1" }
npm view @cueai/omni-reader-mcp@1.3.1 --json
# version=1.3.1, shasum=2bbd51bfd1f5b19a5fda9810ebfe12c44976b1ad, published 2026-08-18T01:09:21.085Z
npm view @cueai/omni-reader-mcp@1.3.1 readme
# present; readme text documents the six public tools and the URL/local parse surface
```

The published 1.3.1 readme lists the public tools as `parse`, `get_parse_status`,
`cancel_parse`, `read_result`, `read_outline`, and `discard_result` — the same six-tool
surface as 1.3.0, unchanged.

## What 1.3.1 fixes (per the release basis)

1.3.1 is a hotfix on the 1.3.0 surface with **no tool-surface change**. The legacy grant
response schema now accepts `omni.parse_grant.v2`:

- cube-mcp's v2 metering writer emits v2 grants for unprofiled local-file requests;
- the 1.3.0 Bridge rejected those with `CUBE_PROTOCOL_ERROR`, which the hotfix resolves.

Everything 1.3.0 documented remains true of 1.3.1: the five existing tool contracts are
unchanged, `read_outline` and URL-result local hydration are present, and `uninstall
--yes --json` removes a trusted 1.3.0 **or** 1.3.1 Bridge entry and restores a matching
trusted URL-only entry when available.

## Release basis

- Published to npm; `dist-tags.latest` confirmed pointing to `1.3.1` by direct registry
  query (not inferred from the publish command's own output).
- Tarball existence verified by download (`omni-reader-mcp-1.3.1.tgz` from
  `https://registry.npmjs.org/@cueai/omni-reader-mcp/-/omni-reader-mcp-1.3.1.tgz`):
  `package/dist/` ships `outline.js`, `source.js`, `remote-client.js`,
  `result-bundle.js`, `tools.js`, and `protocol.js`; `engines.node >=20.12`.
- `npm view` readme presence confirmed for the exact `1.3.1` tag.

## Still unverified (as of 2026-08-18)

- The v2-grant acceptance path was not exercised live; the fix is documented from the
  release basis and readme, not by running a cube-mcp v2 metering-writer request.
- No live client loading or configuration write was performed for this skill release.
- WorkBuddy exact native loading and live acceptance of the skill and the MCP remain
  unverified.

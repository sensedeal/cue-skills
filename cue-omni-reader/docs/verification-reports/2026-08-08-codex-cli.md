# Codex CLI simulated verification — 2026-08-08

- **Skill:** cue-omni-reader v0.1.0
- **Bridge referenced:** 1.1.2
- **Client:** Codex CLI v0.146.0
- **Mode:** simulated; read-only; no production parse or Omni credits

Fresh instruction-text `exec` contexts first received the exact public skill text as the governing instruction for R2, R4, R6, and R8.

In a separate read-only simulated run with no configured Omni MCP surface, JSONL events recorded Codex reading the exact `.agents/skills/cue-omni-reader/SKILL.md` file before answering. This establishes only the observed file read; it does not establish discovery, loading, selection, activation, response behavior, or how Codex reached the file.

| Area | Outcome |
|---|---|
| Root expansion | Requested confirmation for only the containing directory and rejected relocation/upload workarounds. |
| Ambiguous timeout | Refused automatic replacement and required confirmation after a duplicate-billing warning. |
| Artifact lifecycle | Exhausted every cursor, completed the summary, then discarded by default. |
| Tool error classification | Preserved the operation and distinguished a structured tool failure from disconnection. |
| Project skill-file read | JSONL recorded Codex reading only the exact `.agents/skills/cue-omni-reader/SKILL.md` file. |

The four instruction-text behavior checks passed. Separately, JSONL recorded the exact file read described above. No response-behavior pass is attributed to the file-read observation. No production parse occurred and no Omni credits were consumed. Persistent installation, client activation, unprompted automatic triggering, live URL/local-file parsing, artifact handling, billing, cancellation, and cleanup remain unverified.

# Bridge 1.8.3 publication, registry verification, and the data-plane error-code rename — 2026-09-23

- **Bridge version:** `1.8.3`
- **Skill candidate version:** `v0.5.1` (cue-skill publication remains owner-gated)
- **Reviewed Omni Reader source:** `master` `1c0309f2` (tag `0.3.67`; the promotion MR !982 merged !981's bump `049d302b`/`58cfc0c7` on top of the reviewed `test` head `cc7a968f`)
- **Evidence class:** package tests on the exact reviewed tree. npm publication and registry byte verification are recorded below.

## What changed

Bridge 1.8.3 is a pin-sync release with one public-facing disclosure change. The tool surface is unchanged from 1.8.2.

- **Version and trust-set sync.** `package.json` / `package-lock.json` / `BRIDGE_RELEASE_VERSION` move to `1.8.3`; the trusted managed-entry pair advances to **1.8.2 / 1.8.3** (`PREVIOUS_RELEASE_VERSION=1.8.2`); every README npx/npm example and the rollback section pin the new release. The only bare-`npx` Windows migration exception remains 1.5.1.
- **Data-plane error-code rename disclosure.** The README network-diagnostics section now documents that the Bridge's data-plane error codes carry the package's public identity: `OMNI_READER_UNAVAILABLE`, `OMNI_READER_INVALID_RESPONSE`, `OMNI_READER_PARSE_FAILED`, `INSECURE_OMNI_READER_URL`. The rename is a hard cut with no alias; `retryable` / `user_action` per code are unchanged. The public surface continues to carry zero retired internal service tokens (the pack-list guard is itself pinned by `test/package-metadata.test.ts` and stays green).
- No behavior change to `parse`, capability negotiation, or bundle recovery relative to 1.8.2.

## Package publication

The owner published the exact reviewed directory as `@cueai/omni-reader-mcp@1.8.3` on 2026-09-23. Fresh anonymous registry verification established:

- `dist-tags.latest=1.8.3`;
- SHA-1 `2923fe899ad6b160d1c2dff4320a02e11d00ebc1`;
- SHA-256 `82d0cecb1f5aa16bd5ccd47c661cad4d4af474b35f561738402f76cd2bd4fede` (independently computed from a downloaded copy of the published tarball, not only the registry's self-reported `dist.shasum`);
- integrity `sha512-0N0J0t89FEp5+Zcq/rqBRfioCt7Fd/QZIBLg0CNevIR12KGyEL5ekh5LrS8Fwta/NlvfXkVLb6Mq5Lv+Qgp8Xg==`;
- `64 files`, `unpackedSize=658474`;
- registry tarball byte-identical verification passed against the tarball built from the tagged tree (`0.3.67`), and the file lists are equal.

Before publication, the complete package suite ran from a clean checkout of the exact tagged tree: 32 test files, 764 tests passed (exit code judged).

## Review chain

- The rename itself is MR !980 (`test`, merged 2026-09-23, head `cc7a968f`): codex three rounds (r2 block → stale-dist fix with mutation verification → "merge with 1 nit" → nit fixed), review note 50833, pipeline 47292 green.
- The 1.8.3 bump is MR !981 (commit `049d302b`, merge commit `58cfc0c7`, CI pipeline `#3164` green) and the promotion is MR !982 (merge commit `1c0309f2`, CI pipeline `#3166` green). Tag `0.3.67` was cut from `master` `1c0309f2`; its tree pins `"version": "1.8.3"` (verified from the tag object).

## Boundaries

- No API key, authorization header, private source/config path, source content, operation/result handle, account balance, or copied rate conversion is recorded.
- The work used no reset, backfill, or replay.
- Bridge admission uses a minimum-version floor, so 1.8.3 needs no `cube-mcp` admission change.

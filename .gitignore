# Python
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Editor
.vscode/
.idea/
*.swp
*~

# OS
.DS_Store
Thumbs.db

# Cue user-local config (per cue_api.py CONFIG_PATH ~/.cue/config.json — but
# kept here as safety net if anyone places one in repo by accident)
.cue/
config.local.json
*.bak

# Buddy run logs — local artifacts from +test / buddy runs, not source
buddy-run-*.md

# Local Claude Code state (never commit)
.claude/

# local MCP client config (not repo content)
.mcp.json
